[project]
name = "postgres-client"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["psycopg[binary]>=3.2.0", "boto3>=1.35.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
