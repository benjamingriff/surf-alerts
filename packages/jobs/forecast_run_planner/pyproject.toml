[project]
name = "forecast-run-planner"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "boto3>=1.35.0",
  "postgres-client",
  "forecast-control",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
