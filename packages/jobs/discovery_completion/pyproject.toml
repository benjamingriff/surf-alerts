[project]
name = "discovery-completion"
version = "0.1.0"
description = "Reduces discovery spot scrape completion messages into DynamoDB run state"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aws-lambda-powertools>=3.23.0",
    "boto3>=1.42.15",
    "discovery-control"
]

[build-system]
requires = ["uv_build>=0.9.14,<0.10.0"]
build-backend = "uv_build"
